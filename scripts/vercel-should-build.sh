#!/usr/bin/env bash
# Vercel ignoreCommand / Ignored Build Step
# exit 0 = omitir build (no deploy) | exit 1 = continuar build
#
# Los pushes a main y preview no despliegan; publica con npm run vercel:prod o desde el dashboard.

exit 0
