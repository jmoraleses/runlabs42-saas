'use client'

/** PostHog placeholder — enable with NEXT_PUBLIC_POSTHOG_KEY + posthog-js when ready. */
export function PostHogProvider({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}
